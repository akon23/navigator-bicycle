name: navigator_bicycle
description: Working around Navigator 2.0
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  logger: ^2.0.2+1
  provider: ^6.1.1
  bloc_concurrency: ^0.2.2
  collection: ^1.17.2
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
  freezed: ^2.4.5
  freezed_annotation: ^2.4.1
  cupertino_icons: ^1.0.2
  build_runner: ^2.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
